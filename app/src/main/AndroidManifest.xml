<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.carlosjai.tendavirtual">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PedidosAdminActivity" />
        <activity android:name=".RexistroActivity" />
        <activity android:name=".PedidosEnTramiteActivity" />
        <activity android:name=".FacerPedidoActivity" />
        <activity android:name=".DireccionActivity" />
        <activity android:name=".ComprasRealizadasActivity" />
        <activity android:name=".UsuarioActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>